#include <stdio.h>

int main(void)
{
    int x;
    float n, soma = 0;

    printf("Digite um numero natural:\n");
    scanf("%d", &n);

    for(x=1; x<=n; x++)
    {
        soma = (1/x) + soma;
    }

    printf("O resultado da soma foi:%f", soma);
    return(0);
}
